name: Deploy Tools Auth Mailer

on:
  push:
    branches: [main]
    paths:
      - 'workers/tools-auth-mailer/**'
      - '.github/workflows/deploy-tools-auth-mailer.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Cloudflare credentials
        uses: ./.github/actions/cloudflare-token
        with:
          account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          refresh_token: ${{ secrets.CLOUDFLARE_OAUTH_REFRESH_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install wrangler
        working-directory: workers/tools-auth-mailer
        run: npm install
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ env.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: workers/tools-auth-mailer
          command: deploy
